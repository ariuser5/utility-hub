# Utility Hub

A multi-project repository for .NET utilities and APIs.

- All APIs are located in the `apis/` folder.
- Each API project contains its own `src` directory for source code.

See individual project folders for more details.